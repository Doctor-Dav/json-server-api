{
  "name": "test",
  "version": "1.0.0",
  "description": "json-server на Render",
  "main": "server.js",
  "scripts": {
    "start": "json-server --watch db.json --port 4000 --host 0.0.0.0"
  },
  "dependencies": {
    "json-server": "^0.17.3"
  }
}
